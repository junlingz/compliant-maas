# 大模型生产平台设计系统

## 产品定位

这是面向模型工程师、平台管理员和运营人员的生产级 B2B 工作台。界面应让用户在五秒内确认系统状态、待处理异常和下一步操作，并能在不离开工作上下文的情况下完成配置、执行、监控与审计。

## 信息架构

- 左侧导航：稳定呈现工作域和产品模块，不展示宣传信息。
- 顶部工具栏：呈现当前位置、生产环境状态、搜索、任务与通知。
- 主工作区：始终是视觉中心，优先承载表单、表格、监控和编辑器。
- 合规检查器：默认折叠，按需展开，不与主任务争夺首屏空间。
- 详情抽屉：承载日志、条款、历史和对象详情；确认弹窗只用于需要用户决策的动作。

## 设计令牌

- 字体：Inter、PingFang SC、Microsoft YaHei、系统无衬线字体。
- 主色：`#2F6BFF`，只用于主操作、选中和运行状态。
- 文字：`#182033`；次级文字：`#657087`。
- 页面背景：`#F5F6F8`；主表面：`#FFFFFF`；次级表面：`#F8F9FB`。
- 边框：`#E4E7EC`；聚焦环：`rgba(47, 107, 255, .18)`。
- 成功：`#168A5B`；警告：`#B7791F`；危险：`#D14343`；信息：`#2F6BFF`。
- 间距：4 / 8 / 12 / 16 / 24 / 32 px。
- 圆角：控件 6 px；卡片与面板 8 px；圆形状态标签除外。
- 阴影：仅用于浮层、抽屉和少量悬浮反馈，页面结构主要依靠边框与表面层级。

## 组件原则

- 页面标题使用 20–24 px，模块标题使用 16–18 px，正文 13–14 px。
- 表格承担可比较的运营数据；卡片仅用于离散对象、指标或辅助检查器。
- 主按钮每个上下文最多一个；危险操作必须使用危险色并保留确认步骤。
- 表单控件高度保持 36–40 px，标签置于控件上方，错误信息就近呈现。
- 所有交互必须具备 hover、focus-visible、selected、disabled 和状态反馈。
- 移动端以查看、审批和轻量操作为主；宽表格允许容器内滚动，页面本身不得横向溢出。

## 内容规范

- 使用任务语言：创建任务、查看日志、处理异常、保存配置。
- 避免“面向……赋能……”等宣传性描述进入产品操作界面。
- 条款号和覆盖信息作为审计元数据呈现，不作为页面主标题或导航主文案。
