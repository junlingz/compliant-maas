# 大规模预训练框架主 Agent 验收记录

## 验收范围

- 标书范围：`1.3.2.5.1 大规模预训练框架`，截止 `1.3.2.5.2` 前。
- 页面范围：8 个模块、39 个能力组、143 个功能条目、119 个细分交互。
- 运行形态：独立 `index.html`、HTTP、本地 `file://`、GitHub Pages。

## 自动化结果

执行：

```bash
node scripts/qa-prototype.mjs
```

结果：

```json
{
  "moduleCount": 8,
  "sectionCount": 39,
  "requirementCardsVisited": 143,
  "consoleErrors": [],
  "failures": [],
  "passed": true
}
```

## 已验证断言

1. 8 个模块和 39 个能力页签均可进入，143 个条款核验入口均映射并聚焦到真实可见业务控件；右侧核验卡不承载伪 `data-req-id`。
2. 自回归 MoE 架构切换会展示专家数量、路由策略、稀疏激活并更新参数摘要。
3. 越界学习率被字段级校验拦截，不创建任务、不显示成功弹窗。
4. 配置导出读取当前学习率和批次值，不使用硬编码数据。
5. 文图训练预设会真实回填学习率、批次/梯度累积、轮次和精度。
6. Seq2Seq 兼容页展示 Encoder/Decoder 层数、406M 参数量、Self/Cross Attention 头数与隐藏维度。
7. 分布式核心页提供全局 Epoch/样本/Loss/验证准确率曲线、逐节点 GPU/显存/网络/磁盘表、排序与动态聚合频率；多范式校验会更新 YAML 结果。
8. 微调核心页包含训练/验证 Loss、学习率、F1 图表、实时终端及最终性能/时长/资源/能耗报告；GPU 调度展示逐卡指标。
9. 强化学习历史任务切换会改变曲线路径与最终指标；图表缩放会改变 SVG 视窗。
10. 语言/多模态模型类型切换会更新测评任务；自动化报告页含专属指标、混淆矩阵与导出。
11. 模型资产筛选、登记、分类删除、体验日志过滤、用量分析均改变页面内业务状态；服务审计和编排历史提供可筛选表格。
12. 模型组合筛选和文档全文搜索支持真实空结果状态；白皮书下载文件名、MIME 与文件头均为 PDF。
13. 弹窗、抽屉获得焦点，支持焦点循环和 Escape 关闭；总览卡支持键盘激活。
14. 全 39 个能力页的可见表单控件均有可访问名称；移动导航维护 `aria-expanded`、背景 `inert` 和 Tab 焦点闭环。
15. 390px、320px 无页面级横向溢出；页面通过 `file://` 独立打开，无外部 CSS/JS 依赖。
16. 实时日志最多保留 50 行，页面隐藏或暂停滚动时停止追加。

## 静态检查

- `git diff --check`：通过。
- 标书 Heading 7 共 143 项，原型缺失标题数：0。
- 不含 `1.3.2.5.2`、`1.3.2.5.3` 的业务导航或实现。
- 所有可见 `<button>` 均有明确 `data-*` 交互入口。

## 结论

主 Agent 自动化验收通过；合规审查与 UX/前端技术审查均已完成最终定向复审，结论为完全通过，P0/P1/P2 = `0 / 0 / 0`。
